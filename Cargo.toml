[package]
name = "tacky"
version = "0.3.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace]
members = ["tacky-build", "testing", "pbrs"]


[features]
default = ["alloc"]
alloc = []
std = ["alloc"]

[dependencies]
